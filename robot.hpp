#include <iostream>
#include <string>

using namespace std;
class Robot {
    private:
    int arms;
    int legs;
    bool power;
    string name;
    string catchphrase;
    public:
    Robot();
    //create a constructor that takes all of the member information

    //create a function that lets the robot says its catch phrase

    //create all setters and getters

    
};